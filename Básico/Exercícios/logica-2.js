const paisagem = (largura, altura) => largura >= altura ? 'Está no modo paisagem' : 'Não está no modo paisagem'

console.log(paisagem(2400, 1000))

const paisa = (largura, altura) => largura >= altura // o >= é usado para qualificar a imagem quadrada também 

console.log(paisa(1024, 1024))

